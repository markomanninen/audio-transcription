#!/bin/bash
# Simple dev script launcher for Audio Transcription app

cd "$(dirname "$0")"

# Activate virtual environment if it exists
if [ -f ".venv/bin/activate" ]; then
    source .venv/bin/activate
fi

exec python3 dev_runner.py "$@"